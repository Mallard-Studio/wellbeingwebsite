---
27/8/2026 (5)
Task 10 skipped by the founder after the weekly report prototype was rendered and the numbers costed. Three chart candidates checked against dev/gamerswellbeing/web/insights/data/dataset.json before he called it off. Screen time before vs after is dead: the prototype's own banner says the after minutes are a stand-in worth roughly 19% of the real clock, because Firestore only ever received an OS reading for the pre-install baseline. Session length vs mood is dead too: pairing every APP_OPENED with its closing check-in across all users yields only 54 sessions from 12 users, and the four duration bands read 50%, 58%, 40% and 50%, which is noise rather than the finding the page headline claims. The one real chart was how each app leaves you feeling, YouTube 75% pleasant, Instagram 67%, Facebook 59%, over 391 check-ins from 18 users between 16/7 and 10/8, with LinkedIn dropped for having only 3 users behind it; that is also the page title restated, find out which app drops your mood. Founder skipped it anyway, so it joins tasks 8 and 12 under rejected in the marketing report. Correction worth keeping: the dataset is smaller than this spec had been saying. 188 is the count of user records, but only 44 ever produced a log and only 27 have a usable emotion answer, so any future claim sourced from it uses those numbers. Also answered where the chart would have gone, since section ids mean nothing to the founder: the right panel of WHAT THE DATA SAYS, the box headed "And we understand doomscrolling better than anyone else", replacing its four text rows, one of which already asserts the chart's own claim in words. He rejected being addressed in HTML ids at all, "that is not how I comunicate sections names with you", so a memory now maps every id to its on-page label and replies use the label. Not committed, not pushed.
---
27/8/2026 (4)
Task 9 finished and the Firebase rules answered. Founder pasted his deployed Firestore rules and they are byte-identical to firestore.rules in the app repo, so the wide-open users/** and emotionLogs entries are live, not stale. Wrote dev/gamerswellbeing/firestore.rules.proposed, not deployed: delete blocked on users and its subcollections, update and delete blocked on emotionLogs, and every write required to carry the 64-char device hash. Checked against the Kotlin before writing it, since a rule that breaks the live app is worse than the hole: the only .delete() in the app is FieldValue.delete() on a single field at UserRepository.kt:515, which is an update, and check-ins are only ever created at EmotionRepository.kt:131 and :189, so nothing proposed breaks it. Reads deliberately left open because they cannot be closed from a rules file: the app signs nobody in, so Firestore cannot tell the app from a stranger, and closing reads needs signInAnonymously() at first run plus an ownerUid field on the doc, written up as stage 2 at the bottom of the proposed file and leaving decision D1 intact. Deletion form wired: founder sent the link, form id 1FAIpQLScnsTdwAGurb61MKXVxesIqlKv5Sc2jnpBLJ7Spxj28BABN7A with a single required question Your User ID at entry.1338548693. It has no second question, so rather than collect a reason with nowhere to post it the reason field now removes its name attribute and hides its .field wrapper, which is the same class of lie as the false success message and got the same treatment; adding a short-answer question to the form and pasting its entry id into REASON_ENTRY brings the field back on its own. Waitlist form reuse confirmed by the founder, "no one signed in" on the old Oct 2025 list, so no second form. On his call the email field moved above the Join iOS waitlist button on desktop: .wait-row is now a 260px flex column instead of a row, and the mobile override shrank to just width:100% since the column already stacks. He reported index.html broken; it is not, structure checks clean at 62,341 bytes with balanced tags and the hero and #download both render, so it was a missing URL, http://localhost:8765/wellbeingwebsite/index.html against the server serving D:\anas\Work\GW\website. Legal question for task 10 settled and written into the spec as a four-row table: collecting emotion data and training the model on it are both already authorised by privacy policy section 2, an aggregate chart over all 188 users is publishable because aggregate at that size is anonymous, and one user's week needs that user's consent. The founder's own rule, that sharing a weekly report is the user's choice and nothing gets shared against their will, was adopted as the route for a single-user chart, and is the better landing page object anyway. Explained pseudonymous with the chain that makes it so: dataset.json carries no email or name, but its userId is the same SHA-256 device hash that keys the Firestore doc, the doc carries googleUid per decision D3, and googleUid resolves to an email in Firebase Auth. One line to add to legal/privacy-policy.html section 2 whenever it is next touched, publication of anonymous aggregate statistics, not urgent. Chart still unpicked, candidates are catChart, median minutes a day by category and not emotion data at all, and moodChart. Not committed, not pushed.
---
27/8/2026 (3)
Tasks 8 and 12 rejected by the founder, and task 9 half built. Task 8, the "Not a blocker" comparison, was rejected twice over: the brief's copy on quality, and the block itself on audience, since the founder reads an argument against blocker apps as investor material rather than something a regular visitor came for. If it returns it belongs on investors.html with new copy, and none has been written. Task 12, the FAQ, was rejected on audience too: does it block apps, battery drain, Xiaomi/MIUI, is my data sold and what are points for are support questions, not install objections. That kills the only sanctioned home for the marketplace line, so the landing page now carries none of it at all, which is stricter than brief Part 6 asks rather than looser; decision 1 stands and the full story stays on investors.html. A report to the marketing team is owed once the remaining tasks close, split four ways: done as briefed, done with a change from the PM, done completely differently by the PM, and rejected. Task 9 turned out to be already solved: D:\anas\Work\GW\website\embed\index.html, a leftover embeddable signup widget from Oct 2025, posts at a real live Google Form, verified HTTP 200 and titled "Sign up for the Digital wellbeing app", with a required email at entry.1406486002 and an optional comment at entry.813107488. That is exactly the shape the waitlist needed, so no new form was made; only the form id and email entry id change if the founder wants the iOS list kept separate from the old signups. The mailto: button in #download is gone, replaced by an email field plus the same Join iOS waitlist button posting into a hidden iframe on the delete_my_data pattern, with an inline success line and a 15 second timeout that hands back the hello@ address. The founder's .sub copy above it was not touched, em dash included, because only the control changed. New CSS appended at the end of the stylesheet per the source-order rule, plus a .sr-only utility the page did not have, and at 360 the row stacks to full width. The deletion page was worse than recorded: the hidden iframe fires load on Google's 404 as well, so submitting against REPLACE_FORM_ID showed "Request received" for a request that was never received, meaning every user who asked to be forgotten since 16/8/2026 was told it had worked. Its script now holds the three ids as named constants with a configured check; while the placeholders remain, submit is cancelled and the page says the form is not connected and points at info@digitalwellbeingapp.com, and when the ids land the same code wires form.action and both input names at runtime. Google Forms was chosen over Firestore for both because the site is static on GitHub Pages, so Firestore would need the web API key in the page and a publicly writable collection. Which surfaced the real find of the session: firestore.rules in the app repo has allow read, write: if true on both users/{userId}/** and emotionLogs/{logId}, commented "allow all during development" but introduced by commit cfb1d4c "live firebase rules", and project id gwapp-30e03 ships in every APK, so every user's check-ins and screen time are world readable and world writable. Flagged to the founder as outranking the whole landing page list. Also found while reviewing at 360: index.html:869 carries a hardcoded style="width: 730px;" on the #inside intro paragraph, which makes the document scroll sideways on a phone; pre-existing, present in the backup, left unfixed pending his yes. Separately answered his legal questions on task 10: building the model is already authorised by privacy policy section 2, publishing on a marketing page is not, and the exported dataset is clean at 188 users with no email or name but its userId still joins back through the Firestore doc's googleUid to a Firebase Auth email, which is what pseudonymous means. index.html 59,185 -> 62,341 bytes. Backups: _review/index.html.pre-task9-2026-08-27.bak, _review/delete_my_data.index.html.pre-task9-2026-08-27.bak. Not committed, not pushed.
---
27/8/2026 (2)
Removed two capsules from index.html on the founder's call: the green "Validated in alpha · 2026" .r-badge at the top of the #research lead panel, and the "Get the app" .eyebrow above the #download headline. The #download h2 lost its margin-top:22px with it, since that gap only existed to sit under the capsule. Both .r-badge and .eyebrow CSS rules were left in place: .eyebrow is now used only by the new privacy capsule, and .r-badge is still used by investors.html. investors.html was NOT changed and still carries "Validated in alpha · 2026" in its own copy of #research, which breaks the rule that the two pages carry the same block; that is deliberate for now, since the alpha claim is investor material, but it needs a yes or no. index.html 59,352 -> 59,185 bytes. Screenshots: _review/capsules-removed-research.jpg, _review/capsules-removed-download.jpg. Not committed, not pushed.
---
27/8/2026
Task 7 done, but nothing like the brief asked, and the reasoning matters more than the result. Verified the brief's premise against the Android source at D:\anas\Work\GW\dev\gamerswellbeing first and it was wrong: the app does not use the Accessibility permission at all. There is no class extending AccessibilityService anywhere in the Kotlin, no BIND_ACCESSIBILITY_SERVICE, no service declaration in AndroidManifest.xml and no res/xml accessibility config; AppDetectionService.kt:44 extends BaseForegroundService. The accessibility strings in res/values/strings.xml and res/values-ar/strings.xml, including a full Google Play prominent-disclosure block, are referenced by nothing and are dead. What actually detects the foreground app is PACKAGE_USAGE_STATS, Usage Access, polled in UsageStatsPoller.kt:97 via queryEvents; QUERY_ALL_PACKAGES lists installed apps so the user can pick which to track, and SYSTEM_ALERT_WINDOW draws the butterfly overlay. ACCESS_COARSE_LOCATION is real but marketplace-only, requested in RewardsFragment.kt:188 and resolved to a city in GpsCitySource.kt through NETWORK_PROVIDER plus Geocoder. Check-ins do not stay on the device: EmotionRepository.kt:131 writes them to the Firestore emotionLogs collection, and the app also ships Firebase Analytics, Crashlytics, Auth, Storage, Config and Functions. First build was a full #privacy section placed between #loop and #download, three permission cards on the existing .flow/.step grid plus a "What it never reads" panel and a footnote naming location and notifications. The founder rejected it as overwhelming and asked for two lines. Second build was two lines naming Usage Access. He rejected that too, and the reason is the one that sticks: naming Usage Access is Android-only wording, and an iOS version would make it wrong for half the visitors, so the page must describe what the app sees, never which platform permission grants it. Third and shipped version is his own copy, a small disclaimer capsule at the end of #loop, above #download, reading "Wellbeing ONLY sees what you allow it to see: when apps are opened and closed. Wellbeing NEVER sees what is on your screen, what you type, or any phone data." with a Privacy label. Before the capsule he confirmed the section belongs on the landing page at all: brief Part 3 row 7 lists it as Missing with target "Plain-language block naming the Accessibility permission head-on" inside a table of page blocks, and GTM section 8 lists "Privacy, up front and plain" as item 6 of 9 in the landing page structure, between "Not a blocker" and the install block. privacy-policy.html is not part of this task and appears nowhere in the v2 marketing docs except in the unrelated mood-data publication check for task 10. Markup is a flex row: the house .eyebrow chip with its green dot as the label, then the two lines, wrapped in a capsule at border-radius:100px, max-width 900px, 900x94 at desktop; below 880px it stacks and the radius drops to var(--radius) 18px because a 100px radius on a six-line block bulges. Two typos in the founder's copy, "Wellbing" and "what you allows it to see", were shipped verbatim and flagged first, which he rejected: "come on man! fix my typos.. don't make me look stupid!" Both fixed, and the standing rule is now inverted, mechanical errors get corrected silently, meaning and wording are still proposals; both memory files were updated. Still open and unresolved: the line claims Wellbeing never sees "any phone data" while ACCESS_COARSE_LOCATION ships in the manifest today for a marketplace that is not live, so either the permission comes out of the app or that clause narrows. index.html 58,380 -> 59,352 bytes, so the whole of task 7 costs 972 bytes. Page height at 360px 11,132 -> 11,459 px. Backup at _review/index.html.pre-task7-2026-08-26.bak. Screenshots: _review/task7-privacy-note-desktop.jpg, _review/task7-privacy-note-360.jpg. Not committed, not pushed.
---
26/8/2026 (10)
Task 11 done, and expanded past the brief: the QR code follows the visitor down the page instead of sitting once in #download. Three positions, never two visible at once. Hero, a 112px card beside the Get the app button, which lost its arrow. Nav, a 96px card docked just under the sticky bar beside the nav CTA, shown only while neither in-page QR is on screen, driven by one IntersectionObserver over both .qr-scan elements with a -72px top rootMargin. #download, a 112px card under the Play badge. All three encode the same tagged link, utm_source=web with a new utm_medium=qr so desktop scans can be told apart from button taps in the reports; the row was added to marketing/plans/v2-install-attribution-links.md. assets/qr-play.webp is 456 bytes, 392px built for a 112px display, generated with Python qrcode at error correction M, 45 modules, and the file was decoded back with OpenCV to prove the tag survived. The #download pill became the official Google Play badge, cropped out of the existing google-play-app.png at the website root, resized to 400x136 and saved as assets/play-badge.webp, 12 KB lossless because Google's terms forbid modifying the artwork; it displays at 200x68, above their 40px minimum. All QR and badge rules are desktop only at min-width 981px, since a phone cannot scan its own screen and the hero goes single column below 980. Two layout bugs fixed on the way: .hero-ctas had no align-items so the tall QR card stretched the pill button into a circle, and .cta-block needed align-items:flex-start so the waitlist button lines up with the badge instead of floating down beside the QR. Verified at 1536px through the whole scroll and at 360px in the harness, where no QR appears and the sticky bar is untouched. index.html 54,116 -> 58,380 bytes. Backup at _review/index.html.pre-task11-2026-08-26.bak. Screenshots: _review/task11-hero-qr.jpg, task11-nav-qr-scrolled.jpg, task11-download-badge-qr.jpg, task11-360-hero-and-download.jpg. Committed and pushed by the founder mid-session as 43628d2 "QR code to download the app"; that commit carries index.html, both new assets and the first half of these notes.
---
26/8/2026 (9)
Deleted the now-dead .coming-soon CSS from index.html, four rules, on the founder's call. Zero coming-soon references left on that page; investors.html keeps its own copy because it still shows the footnote. index.html 54,649 -> 54,116 bytes. Also investigated task 9, the iOS waitlist: delete_my_data/index.html already proves the pattern, a plain form POSTing to a Google Form formResponse endpoint through a hidden iframe with the iframe load event as the success signal, no backend needed on GitHub Pages. It also has an inline success state and a timeout fallback to a mailto. Found while reading it that the same file ships REPLACE_FORM_ID, entry.REPLACE_USERID_ENTRY and entry.REPLACE_REASON_ENTRY as literal placeholders, so no deletion request submitted since 16/8/2026 has ever reached a form. Nothing on either HTML page links to it, so the traffic is from the Play listing or the app; the founder needs to check that. Task 9 moved from blocked to unblocked-on-tech, now waiting only on the founder creating the Google Forms and sending the IDs. Not committed, not pushed.
---
26/8/2026 (8)
Task 14 done: the landing page no longer sells rewards, points or the marketplace. #solution rebuilt as a real three-step how-it-works, per the brief's block 3 and GTM section 8.3, variant A approved by the founder: 01 / Notice "Reads the mood", 02 / Ask "Asks how you feel", 03 / Signal "Sends the exit sign". Old steps 02 / Reward and 03 / Redeem removed with their trophy and coin icons, replaced by a speech bubble and an exit-door arrow. Step 01 lost the unsourced "Backed by scientific research" line, since #research now sits above it carrying the real alpha numbers. #inside intro lost its "and earn rewards*" tail. #download headline went from "Less screen. Better mood. Real rewards.*" to two lines. All four "Marketplace & points redemption are under construction and coming soon" footnotes deleted, from #solution, #inside, #loop and #download; the spec had recorded only two. Nav and the #loop section left alone on the founder's instruction, only the footnote inside #loop went. investors.html untouched, it keeps the marketplace story per decision 1. index.html 55,983 -> 54,649 bytes, page height at 360px 11,651 -> 11,132 px. Backup at _review/index.html.pre-task14-2026-08-26.bak. Not committed, not pushed.
---
26/8/2026 (7)
Made the #research heading break at the full stop, one line per sentence: each sentence wrapped in span.sentence set to display:block, section-head widened 760px -> 820px so the first sentence fits on one line, text-wrap:balance kept per sentence for narrow screens. Copy untouched. Applied to index.html and investors.html. Not committed, not pushed.
---
26/8/2026 (6)
Founder reversed part of task 4: the What the data says section (#research) is back on the landing page between the hero and #problem, with its count-up animation and a The data nav link. It stays on investors.html as well, so both pages now carry that block and must be edited together. The seven copy edits made to it were reverted on request; the section text is byte-identical to the original on both pages. index.html 51.7 -> 55.7 KB, page height at 360px 10,025 -> 11,651 px. Not committed, not pushed.
---
26/8/2026 (5)
Rewrote specs/v2-landing-page.md as a clean handoff document: current state of both pages, tasks 1 to 5 with their numbers, what is left and what is blocked, an assets table split into referenced and source-only, the findings worth keeping, and the mobile review method with the harness snippet to rebuild it. Corrected the stale claims that the work was uncommitted and that index-preview.html was still the working file.
---
26/8/2026 (4)
Deleted index-preview.html (merged into index.html, nothing unique left in it) and the superseded assets/exit-sign.webp, replaced by exit-sign-1148.webp. Source originals kept: screen-0*.png, solution-exit-sign.jpg, anas.jpg, omnia.jpg are what the WebP files were built from, and logo.png is still used by axis-checkin/index.html. Not committed, not pushed.
---
26/8/2026 (3)
Replaced the index.html title (was "Trade screentime for wellness", old marketplace framing) with "Wellbeing: Find out which app drops your mood" and added the missing meta description. Not committed, not pushed.
---
26/8/2026 (2)
Preview signed off and merged: index-preview.html copied over index.html byte for byte, so the landing page now carries the new hero, the sticky mobile install bar, the UTM-tagged Play links, the cut investor sections and the WebP images. Pre-merge index.html backed up outside the repo at _review/index.html.pre-merge-2026-08-26.bak. index-preview.html is now a stale duplicate and should be deleted. Eight assets/*.webp are still untracked and must be added or the page ships with broken images. Not committed, not pushed.
---
26/8/2026
v2 landing page task 5 done. All landing page images converted to WebP at 2x display size with width/height attributes and lazy loading below the fold: 1,689 KB of images down to 164 KB (-90%), whole page 216 KB, 43 KB of that above the fold. firstrun_hero.gif deliberately stays a GIF, animated WebP came out larger at every quality tried and MP4 needs ffmpeg which is not installed; it is lazy-loaded on investors.html only. No originals deleted, live index.html still references them. Not committed, not pushed.
---
25/8/2026 (3)
v2 landing page task 4 done. Cut #research, #moat and #investors from index-preview.html, removed the two dead nav links and the UAE National Wellbeing Strategy 2031 footer line, added a footer-only Investors link. New investors.html carries that content plus a Why now section that frames Strategy 2031 as market timing and not as endorsement, and the founder photos as avatar cards. index-preview.html 58,820 -> 52,457 bytes. Not committed, not pushed.
---
25/8/2026 (2)
v2 landing page tasks 2 and 3 done in index-preview.html. Task 3: all Play links normalized to the canonical landing-page attribution tag (utm_source=web, utm_medium=site) from marketing/plans/v2-install-attribution-links.md; the hero link had been missing utm_medium. Task 2: sticky install bar, fixed bottom, mobile only below 820px, safe-area padding and body padding-bottom so it never covers the footer, no JS. Verified at 360px top/mid/bottom and hidden on desktop. Not committed, not pushed.
---
25/8/2026
v2 landing page rebuild started for the marketing team. Stripped 1.82 MB of browser ad-blocker CSS from index.html (1,876,271 -> 57,478 bytes, -97%) and closed the missing </body></html>. New hero built in index-preview.html: "YOU NEED AN EXIT SIGN." with assets/exit-sign.webp, no eyebrow capsule, one CTA, UTM-tagged Play link. See specs/v2-landing-page.md for the task list, locked decisions and blockers. Source of truth is D:\anas\Work\GW\marketing\plans\ (brief, GTM plan, voice-guide). Not committed, not pushed.
---
16/8/2026
Added Delete My Data page at delete_my_data/index.html — the Android app (ProfileFragment.kt:96) links to /delete_my_data but the page never existed on this site. Google Form IDs are placeholders (REPLACE_FORM_ID / REPLACE_USERID_ENTRY / REPLACE_REASON_ENTRY) pending a new dedicated deletion form. Not pushed yet.
---
4/8/2026
Added Axis Check-in page at axis-checkin/index.html, URL-only (no site links). Copied from dev repo. See specs/axis-checkin.md. Not pushed yet.
---
21/7/2026 04:30 PM
I removed this DNS line from digitalwellbeingapp.com settings :
CNAME www.digitalwellbeingapp.com ghs.googlehosted.com 600
because I want to redirect it to digitalwellbeing.xyz
---
21/7/2026 03:00 PM
I added the website to public git repo and used pages to host it on digitalwellbeing.xyz
